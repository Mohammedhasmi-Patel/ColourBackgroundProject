/* Premetieve data types and refernce(Non premeitieve ) variable 

premitive : int,  bool, char, string, null ,undefined,Symbol,BigInt


Reference(Non premitive data types ) data types: Array ,Objects,Function 


is javascript dynamically type language or statically type language?

HomeWork

*/

const id = Symbol("123");
const anotherId = Symbol("123");

console.log(id);
console.log(anotherId);

console.log(id === anotherId);
